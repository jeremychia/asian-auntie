name: Keep Alive

on:
  schedule:
    - cron: "17 * * * *"
    - cron: "43 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: |
          DELAY=$(( RANDOM % 1800 ))
          sleep $DELAY
          curl -sf https://asian-auntie.onrender.com/ -o /dev/null
